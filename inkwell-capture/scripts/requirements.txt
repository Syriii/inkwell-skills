# 采集 Skill 依赖

trafilatura>=2.0       # L1 正文提取
requests>=2.28         # HTTP 请求
beautifulsoup4>=4.12   # HTML 解析（论坛抓取）
lxml>=5.0              # BS4 解析器

# L2 Playwright（可选，JS 渲染页面需要）
playwright>=1.40

# OCR（可选，截图文字识别需要）
# paddlepaddle>=3.0    # PaddlePaddle 框架（较大，按需安装）
# paddleocr>=2.7       # PaddleOCR 模型

# OCR 质量评估（可选）
# https://github.com/kpu/kenlm
# kenlm                 # KenLM（需从源码编译或 pip install kenlm）
